export const fetchAllChatsAPI = async () => {
  const response = await fetch()

  // 
  // Return JSON response {data: ..., etc: ...}
} 

export const sendGroupMessageAPI = async (groupName, message) => {
  const response = await fetch()

  // 

}

export const recieveGroupMessagesAPI = async (groupName, page) => {
  // Can ignore page - page 1 returns last 100 messages, no need to implement scrolling for more
  const response = await fetch()

  // 

}